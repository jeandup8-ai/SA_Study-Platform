# Supabase (public, safe to expose in a client bundle — access is enforced by RLS, not by
# keeping these secret). Never put the Supabase *service role* key in frontend code or in git.
#
# Use the LEGACY anon (JWT) key here, not the newer sb_publishable_... key — the newer
# format returned 401s on direct PostgREST calls from the deployed app (see README).
VITE_SUPABASE_URL=https://dzphkuzhdpzawhucmjzh.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImR6cGhrdXpoZHB6YXdodWNtanpoIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODcwNTYwOTgsImV4cCI6MjEwMjYzMjA5OH0.WpJOMMv9tFxv39fSWSE4Fp5rOZlxk8xCUQdTAT3Q45c

# Optional: Sentry error tracking (client-side, public DSN by design).
VITE_SENTRY_DSN=

# --- Not yet wired up (see README for status) ---
# Content moderation provider credentials live server-side only (e.g. a Supabase Edge
# Function), never in this file / never in the client bundle.
# MODERATION_PROVIDER_API_KEY=
# Payment provider (Paystack / Peach Payments / PayFast) credentials, server-side only.
# PAYMENTS_PROVIDER_SECRET_KEY=
